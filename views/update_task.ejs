<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Update Task</title>
    <style>
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            padding: 20px;
            background-color: #f4f4f4;
        }
        form {
            max-width: 600px;
            margin: 0 auto;
            background: #fff;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }
        h1, p {
            text-align: center;
        }
        hr {
            margin-bottom: 20px;
        }
        label {
            display: block;
            margin-top: 10px;
            font-weight: bold;
        }
        input[type="text"], select {
            width: 100%;
            padding: 8px;
            margin-top: 5px;
            margin-bottom: 20px;
            border-radius: 4px;
            border: 1px solid #ccc;
            box-sizing: border-box; /* Ensures padding does not affect overall width */
        }
        .registerbtn {
            width: 100%;
            background-color: #007bff;
            color: white;
            padding: 10px;
            border: none;
            border-radius: 4px;
            cursor: pointer;
        }
        .registerbtn:hover {
            background-color: #0056b3;
        }
    </style>
</head>
<body>
    <form method="post" id="update_task">
        <h1>Update Task</h1>
        <p>Please fill in this form to update the task.</p>
        <hr>
        <label for="number">Task Number :</label>
        <input type="hidden"  name="id" value="<%= task._id%>"><br>
        <input type="text"  name="number" value="<%= task.number%>"><br>

        <label for="name">Name :</label>
        <input type="text" name="name" value="<%= task.name%>"><br>

        <label for="email">E-mail :</label>
        <input type="text" name="email" value="<%= task.email%>"><br>

        <label for="type">Task Type :</label>
        <input type="text" name="type" value="<%= task.type%>"><br>

        <label for="pcnumber">Asset Number :</label>
        <input type="text" name="pcnumber" value="<%= task.pcnumber%>"><br>

        <label for="model">Computer model :</label>
        <input type="text" name="model" value="<%= task.model%>"><br>

        <label for="status">Task status :</label>
        <select name="status" id="status">
            <option value="" disabled>Select status</option>
            <option value="open" <%= task.status === 'open' ? 'selected' : '' %>>open</option>
            <option value="in progress" <%= task.status === 'in progress' ? 'selected' : '' %>>in progress</option>
            <option value="closed" <%= task.status === 'closed' ? 'selected' : '' %>>closed</option>
            <option value="canceled" <%= task.status === 'canceled' ? 'selected' : '' %>>canceled</option>
        </select><br>

        <label for="owner">Technician :</label>
        <input type="text" name="owner" value="<%= task.owner%>"><br>
     
        <button type="submit" class="registerbtn">Save</button><br><br>

    </form>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.7.1/jquery.js" integrity="sha512-+k1pnlgt4F1H8L7t3z95o3/KO+o78INEcXTbnoJQ/F2VqDVhWoaiVml/OEHv9HsVgxUaVW+IbiZPUJQfF/YxZw==" crossorigin="anonymous" referrerpolicy="no-referrer"></script>
    <script src="/public/js/index.js"></script>
</body>
</html>